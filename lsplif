pygmentize -g <<"EOF"
#!/usr/bin/perl
if(@proc) {}
if(scalar @proc >= 1) {}

$d = "" if defined $a and $a eq "";
return  if $f =~ /xxx.*/

#below seems the same
$o = $o//NOORDER
$v //= 'hello'  #if no $v, set $v to 'hello
EOF
