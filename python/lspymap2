pygmentize -g <<"EOF"
#!/usr/bin/python
#check if items in a list exits in a string
alist = ["scala", "akka", "play framework", "sbt", "typesafe"]
tweet = "This is an example tweet talking about scala and sbt."
print map(lambda x: x in tweet.split(), alist)       #[True, False, False, False, False]
print [ x in tweet.split() for x in alist ]          #[True, False, False, False, False]
EOF
